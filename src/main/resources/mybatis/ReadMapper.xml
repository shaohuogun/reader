<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.shaohuogun.reader.read.dao.ReadMapper">
	<resultMap id="listResultMap" type="org.shaohuogun.reader.read.model.ReadingList">
		<id property="id" column="id" />
		<result property="creator" column="creator" />
		<result property="createDate" column="create_date" />
		<result property="lastModifier" column="last_modifier" />
		<result property="lastModifyDate" column="last_modify_date" />
		<result property="deleted" column="deleted" />
		<result property="name" column="name" />
		<result property="description" column="description"
			typeHandler="org.shaohuogun.reader.plugin.ibatis.BlobTypeHandler" />
	</resultMap>

	<resultMap id="listItemResultMap"
		type="org.shaohuogun.reader.read.model.ReadingListItem">
		<id property="id" column="id" />
		<result property="creator" column="creator" />
		<result property="createDate" column="create_date" />
		<result property="lastModifier" column="last_modifier" />
		<result property="lastModifyDate" column="last_modify_date" />
		<result property="deleted" column="deleted" />
		<result property="listId" column="list_id" />
		<result property="bookId" column="book_id" />
		<result property="bookName" column="book_name" />
		<result property="status" column="status" />
	</resultMap>

	<insert id="insertList" parameterType="org.shaohuogun.reader.read.model.ReadingList">
		insert into
		READER_READING_LIST
		(id,
		creator, create_date, deleted, name,
		description)
		values (#{id},
		#{creator}, #{createDate}, #{deleted},
		#{name},
		#{description}
		)
	</insert>

	<select id="selectListById" parameterType="java.lang.String"
		resultMap="listResultMap">
		select * from READER_READING_LIST where id = #{id}
	</select>

	<select id="countListByCreator" parameterType="java.lang.String"
		resultType="java.lang.Integer">
		select count(*) from READER_READING_LIST where deleted =
		'0' and
		creator
		= #{creator}
	</select>

	<select id="selectListsByCreator" parameterType="java.lang.String"
		resultMap="listResultMap">
		select * from READER_READING_LIST where deleted = '0' and
		creator
		= #{creator} order by create_date desc
	</select>

	<update id="updateList" parameterType="org.shaohuogun.reader.read.model.ReadingList"
		flushCache="true">
		update
		READER_READING_LIST set last_modifier =
		#{lastModifier},
		last_modify_date =
		#{lastModifyDate}, deleted =
		#{deleted},
		name = #{name},
		description = #{description}
		where
		id = #{id}
	</update>

	<insert id="insertListItem" parameterType="org.shaohuogun.reader.read.model.ReadingListItem">
		insert into
		READER_READING_LIST_ITEM
		(id,
		creator, create_date, deleted, list_id,
		book_id, book_name, status)
		values (#{id},
		#{creator}, #{createDate},
		#{deleted},
		#{listId},
		#{bookId},
		#{bookName},
		#{status}
		)
	</insert>

	<select id="selectListItemById" parameterType="java.lang.String"
		resultMap="listItemResultMap">
		select * from READER_READING_LIST_ITEM where id = #{id}
	</select>

	<select id="countListItemByCreator" parameterType="java.lang.String"
		resultType="java.lang.Integer">
		select count(*) from READER_READING_LIST_ITEM where deleted
		=
		'0' and
		creator
		= #{creator}
	</select>

	<select id="selectListItemsByCreator" parameterType="java.util.Map"
		resultMap="listItemResultMap">
		select * from READER_READING_LIST_ITEM where deleted = '0'
		and
		creator
		= #{creator} and list_id = #{listId} order by create_date
		desc
	</select>

	<update id="updateListItem" parameterType="org.shaohuogun.reader.read.model.ReadingListItem"
		flushCache="true">
		update
		READER_READING_LIST_ITEM set last_modifier =
		#{lastModifier},
		last_modify_date =
		#{lastModifyDate}, deleted =
		#{deleted},
		status = #{status}
		where
		id = #{id}
	</update>

</mapper>

